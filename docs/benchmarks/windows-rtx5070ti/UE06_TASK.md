# UE-06 — Grok Bot / ling（Windows copy）

Canonical: `docs/development/UE-06_TASK.md`.

UE-05 PARTIAL stop was Cursor’s call, not a crash. Continue.

**Must:** (1) ground walkaround around BoomBox — ≥3 stills on Mac `validation/ue06/` showing floor/contact, not sky; (2) keep Windows `-R` 18793+18794 alive with auto-reconnect after VPS drops; (3) `ue06_closeout.json` on Win logs + Mac validation.

**Don't:** ToggleDebugCamera, Avocado recook, Mac `src/`, P0/P1 claim, tavern, ICE/VPN, sshd, reboot, AIGA/llama.

Then stop.
