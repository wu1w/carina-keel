# Carina v1.0 实施路线

> 2026-09-11：本文阶段排期已由[下一代开发方案 NG-1](docs/NEXT_GENERATION_PLAN.md)替代。新计划先打通 Windows 高质量串流酒馆，再做生成资产、空间编辑和后台扩展；约45个工作日，含返工按10–12周估算。下文保留为原规划和验收背景，不与新计划叠加工期。

日期：2026-09-10。依据：[PRD v1.0](PRD.md)、[模块架构](ARCHITECTURE.md)。

本计划替代 [最初的 20 日计划](docs/archive/NEXT_ITERATION_PLAN-2026-09-10-initial.md)。新范围增加世界 session、真实模型控制、3D 校准固化、空间记忆读回和建模资源导出，不能沿用原工期。本文为规划，不表示任务已经开工或依赖已经部署。

## 1. 交付策略

先验证最难的链路，再扩展产品。首个实验不能只展示一张图或一个 splat，而要证明以下连续过程：

**自然语言创建酒馆 → 指定视角与生成范围 → 校准门高和桌子位置 → 固化 → 关闭生成服务后行走/开门/拿杯子 → GLB 导入建模工具 → 门外生成花园，原酒馆保持不变。**

技术实验允许使用少量人工检查和对象修复，但必须记录人工步骤、耗时和比例；需要人工才能通过的部分不能直接宣称产品全程自然语言完成。

首版首选能提供足够 3D 信息的实际后端。可评估两条路径：

- **路径 A：原生三维生成/导出 → 校准、对象处理 → 运行与导出。** 用于尽早验证完整产品链；不把任一供应商未经验证的接口能力写成承诺。
- **路径 B：可控生成视频 → 多视角采集 → 重建 → 校准、对象处理。** 适配当前 LingBot 方向；优先验证输入是否满足几何一致与覆盖，不因为已有 sidecar 就默认选它作为最终路线。

最终要求至少一条真实路径通过核心验收。路径 B 不可用时可采用 A，但明确当前 LingBot 固化仍未完成；不能把仅能观看的路径降格算作可玩首版。

## 2. 阶段、依赖与工期

估算基于单人全职、至少一个可调用生成后端与重建/三维资产处理环境。顺序合计约 48 个工作日；预留适配与返工后按 10–12 周规划。GPU 重建、对象拆分或局部条件生成若需新研究，应重新估算，不能承诺在原时间内自动解决。

### G0 · 技术可行性关口 / 3 个工作日

目的：决定后端路线，识别阻断产品的技术问题。

工作：

1. 保存现有代码差异和演示包的可恢复基线；记录服务版本，不擅自更换远端其他模型服务。
2. 列出当前后端的文本/相机/动作/参考/取消/续接/导出能力，区分支持、不支持、未验证。
3. 以同一小场景试原生 3D 与视频重建路径中可获取的候选，测试至少六个视角、米制锚点、对象拆分和 GLB 导入。
4. 做一次保护原空间的扩展试验。检查输入中真正存在参考约束，结果中原区域资产未被替换。
5. 记录 CPU/GPU/显存、模型版本、分阶段时延、失败率、人工修复步骤和任务成本；未实测项明确未验证。

交付：`docs/experiments/spatial-memory-spike.md`、测试输入/结果清单（大文件在仓库外）、候选能力报告、路线 ADR。上述文件在执行该阶段时创建。

通过条件：至少一条路径可得到有空间关系的三维候选，说明如何获得碰撞和独立物件；生成控制与扩展有实际证据。三天只能得出路线结论时，保留其余验证项为阻塞，不宣称完整链已通过。

停止条件：所有候选都不能达到几何/独立对象/扩展要求时，形成具体失败证据与最小替代方案；暂停后续重型实现，重新选择技术路线。用户目标不因此改写为视频聊天。

### M1 · WorldSession 与控制基础 / 5 个工作日

依赖：G0 给出可用路线和契约边界。

模块：schema、pack、sessions、application、server、tools。

交付：稳定 worldId、新建/打开/切换、统一命令门面、单写队列、commandId 幂等、runState/controlEpoch、暂停优先通道、session 事件流、v0 备份迁移骨架、全局档案与本世界规则文档的读写提交。先做最小世界列表、运行/暂停入口和规则面板用于验收，不做视觉美化。

通过条件：A1；A5 的时钟与任务隔离部分；A10 的隔离与提交路径切片（规则面板能改、两世界不串台；自然语言补丁可后置到 M3）；两个世界来回切换，迟到结果不串台；paused 状态能接受编辑命令但不自动推进；当前 CLI/MCP 回归仍过。

### M2 · 可玩的空间资产与固化 / 10 个工作日

依赖：M1 与 G0 选定资产路径。

模块：pack、spatial、providers、jobs、runtime、web。

交付：不可变区域版本、规范坐标、相机/深度/资产观测契约、基本校准、viewable/playable/editable 检查、mesh 视口、碰撞/通路、门与独立物件、暂停期检查相机、基础 GLB 导出。网格导出在此阶段就验证，不留到最后发现不能建模。

通过条件：A4、A8 的最小切片；关闭模型后仍可行走/开门/取物；Blender 能分别选中门和至少三件物品，尺度与贴图正确。无网格或对象全烘焙进背景，不能过关。

### M3 · 自然语言生成与校准闭环 / 10 个工作日

依赖：M2 的空间编辑与提交机制可用。

模块：steward、application、spatial、providers、jobs、web。

交付：生成/校准意图计划、当前选中对象的指代、保护范围、参考视图/深度/遮罩、相机动作控制、受约束的区域扩展、必要歧义提问、差异反馈和撤销；自然语言修订规则时给出可展示的文档补丁。生成/校准结果使用 readSet 和 epoch 回到提交队列验证。

通过条件：A2、A3、A6、完整 A10；自然语言移动桌子/调整门高不改其他对象；门外扩展不替换原空间；自然语言改法则与规则面板写入同一文档；后端不支持的能力有真实反馈。用户不需要知道模型 prompt 或对象 ID。

### M4 · 世界运行与管家行为 / 8 个工作日

依赖：M3 已能稳定操作和恢复世界。

模块：runtime、world、steward、web。

交付：世界时间、运行/暂停/单步、有界自然语言导航、对象规则、从 WORLD.md 提取的可执行 WorldRules、一个独立 NPC 的基本动作/对话/知识状态；运行中编辑的短暂停止与恢复。已有区域连续运动不经过逐帧 LLM。

通过条件：完整 A5 与 A9 的行为链；NPC 动作可暂停续接；拿走物品不在背景重复出现；违反本世界法则的玩家动作被拒绝且不改布局；世界运行受预算/时长限制；明确区分停止移动、暂停世界、停止生成。

### M5 · 导出、恢复与故障闭环 / 7 个工作日

依赖：M2 的最小导出与 M4 的完整状态。

模块：pack、exporter、sessions、jobs、application。

交付：一致快照世界包、建模资源包、材质/对象映射、来源与不支持项清单；完整 checkpoint 恢复、导入冲突处理、服务重启恢复任务、预算与自动修复上限；对状态/资产提交做故障注入。

通过条件：A7、完整 A8；从另一目录导入并重开，世界状态与资产一致；导出中世界继续运行仍不混版本；取消或重复请求不重复执行；无临时 URL、机器路径或凭据依赖。

### M6 · 集成验收与可用性 / 5 个工作日

依赖：M1–M5 全部通过。

交付：A1–A10 的自动/人工报告、指定设备性能报告、三位未读代码用户试玩记录、README/操作说明、已知局限。修复关键阻碍后发布可验收原型。

通过条件：全部必选故事通过，且核心环节可全程自然语言完成。mock 测试作为回归基础，不能代替真实生成/校准/导出。凡需手工建模或手工拼接才能完成的必选步骤，标记未完成并排入修复，不能隐藏在演示准备中。

## 3. 第一批可直接开工的任务

### TASK-01：控制与空间固化能力试验

产物：G0 报告和路线 ADR。覆盖生成控制、六视角、尺度锚点、独立对象、离线重访、GLB、边界扩展。禁止只比较画面好不好看。没有某后端访问条件时记录缺口，先验证可用路线，不臆造测试结果。

### TASK-02：WorldSession 与命令协议

产物：schema 契约、session registry、单写命令执行、独立 pause 通道、全局档案与世界规则文档读写；在两个 mock 世界间验证串台/取消/重连/规则不串台，然后接真实 provider。依赖 TASK-01 给出 provider 能力契约；不需要等待全套 UI。

### TASK-03：空间包与 3D 运行切片

产物：由真实生成结果构成的 region/object manifest、可移动视口、碰撞、门/物件、离线重开、基础 GLB。依赖 TASK-01 的资产路线与 TASK-02 的世界身份；禁止用纯手工场景代替真实结果作为最终验收。

之后再拆自然语言校准、约束扩展、NPC 和完整导出任务。每个任务同时包含用户故事、提交/恢复策略与实测方法。

## 4. 验证方法与指标

### 确定性协议测试

至少覆盖：worldId 隔离、同 commandId 幂等、旧 expectedRevision 拒绝、读集冲突、pause epoch 屏障、暂停期间编辑、切换失败、重连补事件、资产未落盘前禁止提交、提交后 HEAD 更新前崩溃、导出固定快照、全局规则不写入世界 MEMORY.md、本世界 WORLD.md 变更不出现在另一世界。

原有 101 个测试保持回归价值；与新状态语义冲突的旧断言按迁移契约修订，不保留“写一句 broken 文本就证明花瓶行为正确”的验收替代。

### 空间与资产验证

固定测试场景：一室一外区，至少六个相机视角、两个尺寸锚点、三个独立物件、一扇门、一条可行走路线。测试 protected 区域引用、尺度、门口接缝、对象 ID、材质和碰撞；人工检查薄结构/遮挡/反射等未被几何指标覆盖的问题。

A3 的 ≤5cm 和 A6 的 ≤10cm 是有尺度锚点样板的目标；未标定输入不冒充满足该精度。动态人物不参与静态重建基准，而是独立对象验收。

### 真实模型与用户测试

覆盖同义指令、指代歧义、创作者修改与玩家动作区别、跨世界检索、旧记忆替代。每轮保留命令、计划、基线、实际控制输入、结果和检查报告，能说明失败发生在哪一层。

三位用户完成 A9；其中至少一次用规则面板或自然语言写下一条世界法则并验证它生效。记录必要澄清次数、失败恢复、长等待和需要开发者介入的位置。不能用 LLM 自评分替代用户实际完成任务。

### 性能与费用

使用 PRD 的本地反馈、pause、固化场景帧率目标。控制类测试至少 100 次记录 p95；视口录制固定路线至少 60 秒，统计中位帧率和 p95 帧耗时。冷启动、热运行、模型不可达分别报告。

生成和重建至少收集可承受预算内的多个重复样本，报告样本数与中位/范围；小样本不冒充稳定 p95。费用按实际调用记录；本计划不授权采购、订阅或升级硬件。

## 5. 关键决策记录

已经确定的产品约束：世界=session、自然语言贯穿、用户可编写全局/世界规则文档、运行与暂停、校准固化、空间记忆读回、可玩离线区域、建模导出。

需要在 G0/M2 固定的工程选择：真实后端路线、GPU/重建部署、3D 显示引擎、碰撞实现、对象拆分和材质处理、保护区扩展方法。每项决定保留试验依据与不支持范围，不让技术选型先于能力验证。

可接受的初期限制：一个活跃世界、有限地图、有限动作集、基础 NPC、生成区域非实时、GLB 优先。不可接受的替代：只保存视频、只保存自然语言、只输出不可交互背景、导出仅含 splat、修改未读回空间记忆。

## English summary

The former 20-day plan is superseded. The proposed scope is approximately 48 working days, or 10–12 weeks with integration contingency, conditional on the initial technical gate.

First prove controllable generation, calibrated spatial assets, offline play, editable export and memory-constrained extension. Then implement persistent sessions, spatial commits, natural-language control, simulation, recovery and user acceptance. A failed reconstruction route must trigger a route decision, not a redefinition of the product as a video chat application.
